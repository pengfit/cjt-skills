
# ── 自动生成: 芯数 ──
m = re.search(r'单芯', s)
if m:
    result['cores']='1芯'

# ── 自动生成: 芯数 ──
m = re.search(r"单芯", s)
if m:
    result["cores"] = "1芯"

# ── 自动生成: 芯数 ──
m = re.search(r"单芯", s)
if m:
    result["cores"] = "1芯"

# ── 自动生成: 芯数 ──
m = re.search(r"单芯", s)
if m:
    result["cores"] = "1芯"

# ── 自动生成: 芯数 ──
m = re.search(r"单芯", s)
if m:
    result["cores"] = "1芯"

# ── 自动生成: 芯数 ──
m = re.search(r"单芯", s)
if m:
    result["cores"] = "1芯"
