
# ── 自动生成: 控制电缆芯数 2 ──
m = re.search(r'-(\d+)×', s)
if m:
    result['cores'] = m.group(1)

# ── 自动生成: 电缆芯数 3 ──
m = re.search(r'-(\d+)×', s)
if m:
    result['cores'] = m.group(1)

# ── 自动生成: 电缆芯数 3 ──
m = re.search(r'-(\d+)×', s)
if m:
    result['cores'] = m.group(1)
