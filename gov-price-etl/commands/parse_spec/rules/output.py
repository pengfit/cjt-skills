
# ── 自动生成: 功率 ──
m = re.search(r"(\d+)W", s)
if m:
    result["output"] = m.group(1) + "W"

# ── 自动生成: 功率 ──
m = re.search(r"(\d+)W", s)
if m:
    result["output"] = m.group(1) + "W"

# ── 自动生成: 功率 ──
m = re.search(r"(\d+)W", s)
if m:
    result["output"] = m.group(1) + "W"

# ── 自动生成: 功率 ──
m = re.search(r"(d+)W", s)\nif m:\n    result["output"] = m.group(1) + "W"

# ── 自动生成: 功率 ──
m = re.search(r"(\\d+)W", s)\nif m:\n    result["output"] = m.group(1) + "W"

# ── 自动生成: 功率 ──
m = re.search(r"(\d+)W", s)\nif m:\n    result["output"] = m.group(1) + "W"
