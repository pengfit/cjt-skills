
# ── 自动生成: 截面积 ──
m = re.search(r"(\d+(?:\.\d+)?)\s*mm2", s)
if m:
    result["cross_section"] = m.group(1) + "mm²"

# ── 自动生成: 截面积 ──
m = re.search(r"(\d+(?:\.\d+)?)\s*mm2", s)
if m:
    result["cross_section"] = m.group(1) + "mm²"

# ── 自动生成: 截面积 ──
m = re.search(r"(\d+(?:\.\d+)?)\s*mm2", s)
if m:
    result["cross_section"] = m.group(1) + "mm²"
