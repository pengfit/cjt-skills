
# ── 自动生成: 低烟无卤阻燃 WDZ ──
m = re.search(r'(WDZ|WDZAN)', s)
if m:
    result['series'] = 'WDZ'

# ── 自动生成: 低烟无卤阻燃 WDZ ──
m = re.search(r'(WDZ|WDZAN)', s) if m:     result['series'] = 'WDZ'

# ── 自动生成: 低烟无卤阻燃耐火 WDZN ──
m = re.search(r'(WDZN)', s)
if m:
    result['series'] = 'WDZN'
